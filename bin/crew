#!/usr/bin/env python3
import pathlib, sys
sys.path.insert(0, __import__("os").environ.get("CREW_LIB_ROOT", str(pathlib.Path(__file__).resolve().parents[1] / "lib")))
from crew.core import safe_main
safe_main()
