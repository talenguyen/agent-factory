---
name: autonomous-build
description: Software-pinned alias for autonomous-goal. Use when starting a standalone software build from an idea, feature, or bug that requires plan approval, a risk gate, and final delivery. Not for small in-flight edits already scoped in the current conversation.
user-invocable: true
---

# autonomous-build

Set `DOMAIN=software` and invoke `autonomous-goal` with the same goal. This alias preserves existing software workflows; use `autonomous-goal` for domain-selected work.
